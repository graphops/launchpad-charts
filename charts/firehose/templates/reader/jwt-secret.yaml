{{- $values := $.Values.reader }}
{{- $componentName := "reader" }}
{{- $componentLabel := include "firehose.componentLabelFor" $componentName }}

{{- if $values.jwt.fromLiteral }}
---
apiVersion: v1
type: Opaque
kind: Secret
metadata:
  name: {{ include "firehose.fullname" . }}-{{ $componentName }}-jwt
  labels:
    {{- include "firehose.labels" . | nindent 4 }}
    {{- $componentLabel | nindent 4 }}
data:
  jwt.hex: {{ $values.jwt.fromLiteral | b64enc }}
{{- end }}
