{{- if $.Values.grafana.dashboards }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "erigon.fullname" . }}-dashboards
  labels:
    {{- include "erigon.labels" . | nindent 4 }}
    grafana_dashboard: "argocd" # TODO this should be cinfigrable
data:
{{- (.Files.Glob "dashboards/*").AsConfig | nindent 2 }}
{{- end }}